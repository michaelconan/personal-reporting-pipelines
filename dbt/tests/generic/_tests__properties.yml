---
# Generic tests defined under tests/generic/ are self-documented via their SQL
# header comments. Macro documentation patches for them are dropped because the
# dbt 2.x Rust engine does not resolve macros-path patches for test macros
# (dbt1090 MacroPatchNotFound); keeping a patch here only produces noise.
version: 2